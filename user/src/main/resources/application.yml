server:
  port: 8082

spring:
  application:
    name: user-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # Endereço do Eureka Server
  instance:
    hostname: localhost